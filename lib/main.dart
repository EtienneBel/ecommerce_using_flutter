import 'package:ecommerce_using_flutter/app.dart';
import 'package:flutter/material.dart';

/// ---- Entry point of the application
void main() {
  /// Add widgets binding
  /// init local storage
  /// await native splash
  /// initialize firebase
  /// initialize authentication
  runApp(const App());
}
